1. to compile or add all files to send to git - git add .

2. to commit that files                       - git commit -m "give remarks here"

3. to actually push the code                  - git push -u origin <brach name>


CREATE TABLE "Data_manager_patient_treatment_details" ("id" bigint NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, "patient_id" varchar(11) NOT NULL, "n_th_treatment" integer NOT NULL, "problem_detail" text NOT NULL, "symptoms" text NOT NULL, "treatment_given" text NOT NULL, "medicine_prescribed" text NOT NULL, "fees_charged" numeric(10, 2) NOT NULL, "fees_paid" numeric(10, 2) NOT NULL, "fees_remaining" numeric(10, 2) NOT NULL, "current_checkup_date" date NOT NULL, "next_checkup_date" date NOT NULL, "remarks" text NOT NULL);
COMMIT;